services:
  agent-space:
    build: .
    ports:
      - "18790:18790"
    # Uncomment to mount live data or custom config:
    # volumes:
    #   - ~/.openclaw/agents:/root/.openclaw/agents:ro
    #   - ./config.json:/app/config.json:ro
    environment:
      - BIND_HOST=0.0.0.0
