services:
  agent-space:
    build: .
    ports:
      - "18790:18790"
    volumes:
      # Mount your OpenClaw agents directory for live data
      # - ~/.openclaw/agents:/root/.openclaw/agents:ro
      # Mount a config file for custom agent names/colors
      # - ./config.json:/app/config.json:ro
      []
    environment:
      - BIND_HOST=0.0.0.0
